export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-content flex-col items-center justify-center gap-4 px-4 text-center">
      <h1 className="font-display text-2xl text-ink">SUN-DRAM Scanner</h1>
      <p className="max-w-reading text-ink-muted">
        The scan box, countdown, and result pages land in a later build step. Infrastructure is
        up.
      </p>
    </main>
  );
}
