"""One-off operational commands — not mounted on the FastAPI app, not part
of the JSON contract, run manually (`python -m app.commands.<name>`) from
inside the api container. `migrate_waitlist` (Step 8) is the first."""
